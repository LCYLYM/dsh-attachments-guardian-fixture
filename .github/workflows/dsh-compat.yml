name: DSH compatibility guardian

on:
  schedule:
    - cron: '17 */6 * * *'
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - .dsh-compat.yml
      - .dsh-compat.lock.json
      - compatibility/dsh-smoke.yml
      - .github/workflows/dsh-compat.yml

permissions:
  contents: read

jobs:
  guardian:
    # Guardian full campaign workflow; only change this SHA during an explicit upgrade.
    uses: LCYLYM/dsh-plugin-compat-guardian/.github/workflows/guardian.yml@28898380f3f6a03dab99027ef86850e999f3627b
    permissions:
      contents: write
      pull-requests: write
      issues: write
    with:
      guardian_repository: LCYLYM/dsh-plugin-compat-guardian
      guardian_ref: 28898380f3f6a03dab99027ef86850e999f3627b
      runner: ubuntu-24.04
    secrets:
      deepseek_api_key: ${{ secrets.DEEPSEEK_API_KEY }}
      publisher_token: ${{ secrets.DSH_GUARDIAN_PUBLISH_TOKEN }}
      email_gateway_url: ${{ secrets.GUARDIAN_EMAIL_GATEWAY_URL }}
      telegram_bot_token: ${{ secrets.GUARDIAN_TELEGRAM_BOT_TOKEN }}
      telegram_chat_id: ${{ secrets.GUARDIAN_TELEGRAM_CHAT_ID }}
      notification_webhook_url: ${{ secrets.GUARDIAN_WEBHOOK_URL }}
